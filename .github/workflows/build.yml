name: Build

# ─── Triggers ────────────────────────────────────────────────────────────────
on:
  workflow_dispatch:
    inputs:
      openwrt_version:
        description: >
          OpenWrt Release Version (Note: latest means build against latest release, not latest commit)
        required: true
        default: 'latest'

      target:
        description: 'Platform Target'
        required: true
        default: 'x86'

      subtarget:
        description: 'Platform Subtarget'
        required: true
        default: '64'

      package_format:
        description: >
          Package Format
        required: true
        default: 'ipk'
        type: choice
        options:
          - ipk
          - apk

# ─── Global defaults ─────────────────────────────────────────────────────────
defaults:
  run:
    shell: bash

jobs:
  # ── 1. Resolve "latest" to a concrete version tag ──────────────────────────
  resolve-version:
    name: Resolve OpenWrt version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.resolve.outputs.version }}

    steps:
      - name: Resolve version tag
        id: resolve
        env:
          INPUT_VERSION: ${{ inputs.openwrt_version }}
          GH_TOKEN: ${{ github.token }}
        run: |
          if [[ "${INPUT_VERSION}" == "latest" ]]; then
            VERSION=$(gh release view --repo openwrt/openwrt --json tagName -q '.tagName' | sed 's/^v//')
            echo "Resolved 'latest' → ${VERSION}"
          else
            # Strip a leading 'v' if the user included one
            VERSION="${INPUT_VERSION#v}"
          fi

          # Basic sanity-check: must look like X.Y.Z
          if ! [[ "${VERSION}" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::Version '${VERSION}' does not match expected X.Y.Z format."
            exit 1
          fi

          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

  # ── 2. Build the package ───────────────────────────────────────────────────
  build:
    name: Build opensoho (${{ needs.resolve-version.outputs.version }} · ${{ inputs.target }}/${{ inputs.subtarget }} · ${{ inputs.package_format }})
    needs: resolve-version
    runs-on: ubuntu-22.04

    env:
      OPENWRT_VERSION: ${{ needs.resolve-version.outputs.version }}
      TARGET:          ${{ inputs.target }}
      SUBTARGET:       ${{ inputs.subtarget }}
      PKG_FORMAT:      ${{ inputs.package_format }}
      PKG_NAME:        opensoho

    steps:
      # ── 2a. Validate APK version requirement ──────────────────────────────
      - name: Validate APK/version compatibility
        if: inputs.package_format == 'apk'
        run: |
          MAJOR=$(echo "${OPENWRT_VERSION}" | cut -d. -f1)
          MINOR=$(echo "${OPENWRT_VERSION}" | cut -d. -f2)
          # APK support was introduced in 24.10
          if (( MAJOR < 24 )) || ( (( MAJOR == 24 )) && (( MINOR < 10 )) ); then
            echo "::error::APK package format requires OpenWrt 24.10 or newer (got ${OPENWRT_VERSION})."
            exit 1
          fi

      # ── 2b. Host build dependencies ───────────────────────────────────────
      - name: Install host build dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            build-essential clang flex bison g++ gawk \
            gcc-multilib g++-multilib gettext git \
            libncurses5-dev libssl-dev python3-distutils \
            python3-setuptools rsync swig unzip zlib1g-dev \
            file wget curl jq

      # ── 2c. Discover the SDK tarball URL ──────────────────────────────────
      - name: Discover SDK download URL
        id: sdk
        run: |
          BASE="https://downloads.openwrt.org/releases/${OPENWRT_VERSION}/targets/${TARGET}/${SUBTARGET}"

          # The HTML listing contains the SDK filename; grep for it.
          LISTING=$(curl -fsSL "${BASE}/")

          SDK_FILE=$(echo "${LISTING}" \
            | grep -oP 'openwrt-sdk-[^"<>\s]+\.tar\.(xz|zst)' \
            | head -1)

          if [[ -z "${SDK_FILE}" ]]; then
            echo "::error::No SDK tarball found at ${BASE}/"
            echo "Listing snippet:"
            echo "${LISTING}" | grep -i sdk || echo "(nothing matched)"
            exit 1
          fi

          echo "sdk_url=${BASE}/${SDK_FILE}"   >> "$GITHUB_OUTPUT"
          echo "sdk_file=${SDK_FILE}"           >> "$GITHUB_OUTPUT"
          # Strip both .tar.xz and .tar.zst for the directory name
          SDK_DIR="${SDK_FILE%.tar.*}"
          SDK_DIR="${SDK_DIR%.tar}"
          echo "sdk_dir=${SDK_DIR}"             >> "$GITHUB_OUTPUT"
          echo "Found SDK: ${SDK_FILE}"

      # ── 2d. Cache the extracted SDK ───────────────────────────────────────
      - name: Cache SDK
        id: cache-sdk
        uses: actions/cache@v4
        with:
          path: sdk/
          # Key includes the exact SDK filename, which encodes version + gcc ver
          key: sdk-${{ steps.sdk.outputs.sdk_file }}

      - name: Download & extract SDK
        if: steps.cache-sdk.outputs.cache-hit != 'true'
        run: |
          wget -q --show-progress "${{ steps.sdk.outputs.sdk_url }}" \
               -O sdk_download

          mkdir -p sdk
          tar -xf sdk_download --strip-components=1 -C sdk
          rm sdk_download

      # ── 2e. Check out the current repo so feeds can reference it ──────────
      - name: Check out this repository
        uses: actions/checkout@v4
        with:
          path: src/openwrt-opensoho

      # ── 2f. Configure feeds ───────────────────────────────────────────────
      - name: Set up feeds
        working-directory: sdk
        run: |
          # Keep official feeds (packages, luci, etc.) and append ours.
          # We use src-link so the feed reads directly from the checkout above;
          # no extra network round-trip and always reflects the current commit.
          echo "src-link opensoho_pkg ${{ github.workspace }}/src/openwrt-opensoho" \
            >> feeds.conf.default

          ./scripts/feeds update -a
          # Install only our feed's packages to avoid conflicts
          ./scripts/feeds install -a -p opensoho_pkg
          # Also install golang from the packages feed (build dependency)
          ./scripts/feeds install golang

      # ── 2g. Generate .config ──────────────────────────────────────────────
      - name: Configure build
        working-directory: sdk
        run: |
          # Select our package for compilation
          echo "CONFIG_PACKAGE_${PKG_NAME}=m" >> .config

          # Package format
          if [[ "${PKG_FORMAT}" == "apk" ]]; then
            echo "CONFIG_USE_APK=y" >> .config
          else
            echo "# CONFIG_USE_APK is not set" >> .config
          fi

          make defconfig 2>&1 | grep -v "^#" | head -20 || true

      # ── 2h. Build ─────────────────────────────────────────────────────────
      - name: Build package
        working-directory: sdk
        run: |
          make "package/${PKG_NAME}/compile" \
               V=s \
               -j$(nproc) \
               2>&1 | tee /tmp/build.log | tail -30

          # Surface errors loudly if make failed
          if [[ ${PIPESTATUS[0]} -ne 0 ]]; then
            echo "::error::Build failed — last 60 lines of log:"
            tail -60 /tmp/build.log
            exit 1
          fi

      # ── 2i. Collect artifacts ─────────────────────────────────────────────
      - name: Collect built packages
        id: collect
        run: |
          mkdir -p artifacts

          EXT="${PKG_FORMAT}"
          find sdk/bin/packages -name "*.${EXT}" | while read -r f; do
            cp -v "$f" artifacts/
          done

          COUNT=$(ls artifacts/ | wc -l)
          echo "count=${COUNT}" >> "$GITHUB_OUTPUT"

          if [[ "${COUNT}" -eq 0 ]]; then
            echo "::error::No .${EXT} files found under sdk/bin/packages/"
            find sdk/bin/ -type f | head -20
            exit 1
          fi

          echo "Built packages:"
          ls -lh artifacts/

      # ── 2j. Upload ────────────────────────────────────────────────────────
      - name: Upload packages
        uses: actions/upload-artifact@v4
        with:
          name: >-
            opensoho_${{ env.OPENWRT_VERSION }}_${{ inputs.target }}_${{ inputs.subtarget }}_${{ inputs.package_format }}
          path: artifacts/
          if-no-files-found: error
          retention-days: 30

      # ── 2k. Job summary ───────────────────────────────────────────────────
      - name: Write job summary
        if: always()
        run: |
          cat >> "$GITHUB_STEP_SUMMARY" <<EOF
          ## opensoho build result

          | Field          | Value |
          |----------------|-------|
          | OpenWrt version | \`${OPENWRT_VERSION}\` |
          | Target          | \`${TARGET}/${SUBTARGET}\` |
          | Package format  | \`${PKG_FORMAT}\` |
          | Packages built  | \`${{ steps.collect.outputs.count }}\` |

          $(ls artifacts/ 2>/dev/null | sed 's/^/- `/' | sed 's/$/`/')
          EOF
